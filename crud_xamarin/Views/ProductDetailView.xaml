<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:viewmodels="clr-namespace:crud_xamarin.ViewModels"
             x:Class="crud_xamarin.Views.ProductDetailView"
             x:DataType="viewmodels:ProductDetailViewModel"
             Background="White">

    <StackLayout Padding="10">
        <Label Text="{Binding Product.Id}"
               Style="{StaticResource LabelDefaultStyle}"/>
        <Label Text="{Binding Product.Name}"
               Style="{StaticResource LabelDefaultStyle}"/>
        <Label Text="{Binding Product.Description}"
               Style="{StaticResource LabelDefaultStyle}" TextColor="Black"/>
        <Label Text="sss" TextColor="Black"/>
    </StackLayout>

</ContentPage>